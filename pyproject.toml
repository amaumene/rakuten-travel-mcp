[project]
name = "rakuten-travel-mcp"
version = "0.1.0"
description = "MCP server for Rakuten Travel APIs — search hotels, check availability, get rankings and area codes across Japan."
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "httpx",
]
